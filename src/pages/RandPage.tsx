// PLACEHOLDER — owned by UI agent 3 per docs/SPEC.md. Replace with the real Rand & Inflation page.
import { PageHeader } from '../components/ui'

export default function RandPage() {
  return <PageHeader title="Rand & inflation" intro="Coming soon: USD/ZAR history, CPI vs personal inflation, hedge projections." />
}
