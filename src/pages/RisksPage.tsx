// PLACEHOLDER — owned by UI agent 3 per docs/SPEC.md. Replace with the real Risks page.
import { PageHeader } from '../components/ui'

export default function RisksPage() {
  return <PageHeader title="Risks" intro="Coming soon: risk flags, case studies and SA indicators." />
}
